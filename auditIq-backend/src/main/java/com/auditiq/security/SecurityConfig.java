package com.auditiq.security;

public class SecurityConfig {
    // TODO: implement SecurityFilterChain once spring-boot-starter-security is added
    // Will configure: permit /api/auth/**, JWT filter chain, stateless sessions,
    // CORS, disable CSRF
}
